{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "https://github.com/hhkaos/arcgis-json-schemas/Extent.reference.schema.json",
    "title": "Extent",
    "description": "An extent is a bounding rectangle with points that delineate an area for a map or scene.",
    "type": "object",
    "properties": {
        "xmin": {
            "description": "The minimum X-coordinate of an extent envelope.",
            "type": "number"
        },
        "ymin": {
            "description": "The maximum Y-coordinate of an extent envelope.",
            "type": "number"
        },
        "xmax": {
            "description": "The minimum X-coordinate of an extent envelope.",
            "type": "number"
        },
        "ymax": {
            "description": "The maximum Y-coordinate of an extent envelope.",
            "type": "number"
        }
    },
    "required": ["xmin", "ymin", "xmax", "ymax"]
}
